general:
  branches:
    ignore:
      - gh-pages
machine:
  node:
    version: 6.0.0
dependencies:
  pre:
    - npm i -g npm codecov nyc
    - npm i js-data@^3.0.0-beta.8 gcloud
test:
  pre:
    - echo $KEYFILE > key.json
  post:
    - nyc report --reporter=lcov | codecov
